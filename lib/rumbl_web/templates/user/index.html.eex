<h1> Mostrando usuários </h1>

<table>
  <%= for user <- @users do %>
    <tr>
      <td><b><%= first_name(user) %></b> (<%= user.id %>)</td>
      <td><%= link "Ver", to: Routes.user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table
